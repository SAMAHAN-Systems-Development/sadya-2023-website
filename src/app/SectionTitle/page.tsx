import type { Metadata } from 'next';

import SectionTitle from '@/components/ui/SectionTitle';

export default function Home() {
  return (
    <main>
      <SectionTitle title="patrick" subtitle="matayabas" />
    </main>
  );
}
